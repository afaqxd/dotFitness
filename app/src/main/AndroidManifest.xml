<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dotfitness">

    <application
        android:allowBackup="true"
        android:icon="@drawable/img_appicon2"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DotFitness">
        <activity android:name=".MeditationTimerActivity"></activity>
        <activity android:name=".TimerActivity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".BmiActivity" />
        <activity android:name=".ExercisesActivity"></activity>
        <activity android:name=".SplashScreen">

        </activity>
        <activity android:name=".MainActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>